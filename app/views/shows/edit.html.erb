<article class="type-system-sans">
  <p class="type">Edit <%= @show.when %></p>

  <%= form_for @show do |f| %>
    <%= error_messages_for :new_show %>

    <%= render "form", form: f %>

    <%= f.submit %>
  <% end %>

</article>
