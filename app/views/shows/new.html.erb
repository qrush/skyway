<% @title = 'Add new show' %>

<%= form_for @show do |f| %>
  <%= f.error_messages %>

  <%= render "form", form: f %>

  <%= f.hidden_field :unknown_setlist, value: true %>

  <%= f.submit %>
<% end %>
